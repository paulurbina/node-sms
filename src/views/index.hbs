<div class="row">
    <div class="col-md-4">
        <form action="/send-sms" method="post" class="card card-body">
            <div class="form-group">
                <input type="text" name="message" class="form-control" placeholder="Write your message" autofocus>
            </div>
            <div class="form-group">
                <input type="tel" name="phone" class="form-control" placeholder="+12312312313" autocomplete="off">
            </div>
            <div class="form-group">
                <button class="btn btn-primary btn-block">Send</button>
            </div>
        </form>
    </div>
    <div class="col-md-8" >
        {{#if messages}}
        <ul class="list-group" style="height: 70vh; overflow-y: auto;" id="messages">
            {{#each messages}}
                <div class="list-group-item list-group-item-info list-group-item-action">
                    <p>{{Body}}</p>
                    <span>{{hideNumber To}}</span>
                    <span>{{hideNumber From}}</span>
                    <span>{{timeago createdAt}}</span>
                </div>
            {{/each}}
        </ul>

        {{else}}
        <p>No messages yet</p>
        {{/if}}
    </div>
</div>